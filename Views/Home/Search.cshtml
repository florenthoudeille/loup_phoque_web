@model List<string>

<h2>Résultats de recherche</h2>

@if (Model.Count == 0)
{
    <p>Aucun résultat trouvé.</p>
}
else
{
    <ul>
        @foreach (var service in Model)
        {
            <li>@service</li>
        }
    </ul>
}
